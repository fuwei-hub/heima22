package com.itheiha.test;

import java.util.ArrayList;

/*
19题答案
(1)  String name;
     int score;

     public Student(String name, int score) {
        this.name = name;
        this.score = score;
    }

 (2)创建一个ArrayList集合，这个集合里面存储的是Student类型，分别将上面的三个Student对象添加到集合中
    ArrayList<Student> list = new ArrayList<>();
    list.add(s1);
    list.add(s2);
    list.add(s3);

 (3)遍历Arraylist集合，获取每个学生的成绩并求和
   for (int i = 0; i < list.size(); i++) {
   //list.get(i)
         sum+=list.get(i).getScore ;
        }

 */
public class Task02 {


}
